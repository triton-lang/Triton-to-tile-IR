diff a/third_party/cutile/lib/TritonToCuTile/TritonToCudaTilePass.cpp b/third_party/cutile/lib/TritonToCuTile/TritonToCudaTilePass.cpp	(rejected hunks)
@@ -213,9 +213,10 @@ public:
     newPrefix += "\n";
 
     // create new print op
-    rewriter.create<cuda_tile::PrintTkoOp>(op.getLoc(), newPrefix, args,
-                                           /*token=*/Value());
-    rewriter.eraseOp(op);
+    auto newPrintOp =
+        rewriter.create<cuda_tile::PrintOp>(op.getLoc(), newPrefix, args);
+    rewriter.replaceOp(op, newPrintOp);
+
     return success();
   }
 };
@@ -644,11 +645,11 @@ public:
     } else if (op.getSymbol() == "__nv_tanhf" ||
                op.getSymbol() == "__nv_tanh") {
       rewriter.replaceOpWithNewOp<cuda_tile::TanHOp>(
-          op, adaptor.getSrcs()[0], cuda_tile::RoundingMode::FULL);
+          op, adaptor.getSrcs()[0]);
       return success();
     } else if (op.getSymbol() == "__nv_fast_tanhf") {
       rewriter.replaceOpWithNewOp<cuda_tile::TanHOp>(
-          op, adaptor.getSrcs()[0], cuda_tile::RoundingMode::APPROX);
+          op, adaptor.getSrcs()[0]);
       return success();
     }
     return failure();
